//! Type definitions for {{ contract.name }} NFT Contract
//! Generated by HEAVYMETA Soroban Generator

use soroban_sdk::{contracttype, contracterror, Address, String};

/// Contract error types
#[contracterror]
#[derive(Copy, Clone, Debug, Eq, PartialEq)]
#[repr(u32)]
pub enum Error {
    /// Contract has not been initialized
    NotInitialized = 1,
    /// Contract is already initialized
    AlreadyInitialized = 2,
    /// Caller is not authorized for this action
    Unauthorized = 3,
    /// Token ID does not exist
    InvalidTokenId = 4,
    /// Maximum supply has been reached
    MaxSupplyReached = 5,
    /// Value is out of allowed range
    OutOfRange = 6,
    /// Caller is not the token owner
    NotOwner = 7,
    /// Invalid recipient address
    InvalidRecipient = 8,
}

/// NFT token data structure
#[contracttype]
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct Nft {
    /// Token owner address
    pub owner: Address,
    /// Unique token identifier
    pub id: u64,
    /// IPFS CID for metadata (from Pintheon)
    pub metadata_cid: String,
}

/// Value property configuration
#[contracttype]
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct ValueProperty {
    /// Default value when minted
    pub default: u64,
    /// Minimum allowed value
    pub min: u64,
    /// Maximum allowed value
    pub max: u64,
    /// Increment/decrement step size
    pub amount: u64,
}

{% for name, prop in val_props.items() %}
// {{ name | capitalize }} property configuration constants
pub const {{ name | upper }}_DEFAULT: {{ prop.value_type }} = {{ prop.default }};
pub const {{ name | upper }}_MIN: {{ prop.value_type }} = {{ prop.min }};
pub const {{ name | upper }}_MAX: {{ prop.value_type }} = {{ prop.max }};
pub const {{ name | upper }}_AMOUNT: {{ prop.value_type }} = {{ prop.amount }};

{% endfor %}
